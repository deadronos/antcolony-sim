import { describe, it, expect } from 'vitest';
import { Ants3D } from './Ants3D';

describe('Ants3D Component', () => {
    it('should be a valid React component function', () => {
        expect(typeof Ants3D).toBe('function');
    });
});
